// Linked Lists SINGLY and DOUBLY
// An object that includes object that point to other objects within the class

// pros
    // 

// Cons
    // 

// Time Complexity
    // prepend - O(1)
    // append - O(1)
    // lookup - O(n)
    // inset - O(n)
    // delete - O(n)

// Pointer - a reference to another point in memory
